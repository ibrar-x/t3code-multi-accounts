export type SettingsSurfaceMode = "page" | "modal";

// Toggle this to "modal" to render the settings screen in a modal surface
// while keeping the full /settings page route intact.
export const SETTINGS_SURFACE_MODE: SettingsSurfaceMode = "page";
